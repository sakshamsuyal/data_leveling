import pandas as pd

def combine_two_tables(person: pd.DataFrame, address: pd.DataFrame) -> pd.DataFrame:
   df1 = pd.DataFrame(person)
   df2 =  pd.DataFrame(address)
   df3 = df1.merge(df2,on='personId', how= 'left')
   return df3[['firstName','lastName','city','state']]
